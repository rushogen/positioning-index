Captured 2026-08-07T09:45:44Z with scripts/probe.js's user agent.
These are verbatim robots.txt bodies from sites in seed/companies.json. They
exist so the parser is pinned against real-world formatting -- Content-Signal
lines, bare 'Disallow:', per-bot blocks -- and not only against textbook
examples. Refresh by re-running the curl in tests/robots.test.js's header.
